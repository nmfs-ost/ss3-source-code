on:
  workflow_dispatch:
  push:
    paths:
      - '**.tpl'
    branches: [main]
  pull_request:
    paths:
      - '**.tpl'
    branches: [main]
name: call-build-ss3
jobs:
  call-workflow:
    uses: nmfs-stock-synthesis/workflows/.github/workflows/run-ss3-mcmc.yml@main