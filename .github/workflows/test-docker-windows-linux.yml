name: docker-windows-linux

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@main
    - name: run
      run: |
        choco install docker-desktop
        "C:\Program Files\Docker\Docker\Docker Desktop.exe"
        docker pull johnoel/admb:linux
